db:
  username: test
  password: test

spring:
  datasource:
    username: test
    password: test

  jpa:
    properties.hibernate:
      hbm2ddl.auto: validate

  liquibase:
    change-log: db/changelog/db.changelog-master-test.xml

  security:
    basic:
      disable